# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'm.tolstikov' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '~> 4.7'
    pod 'SwiftLint'
    pod 'Fabric', '~> 1.7'
    pod 'Crashlytics', '~> 3.10'
  # Pods for m.tolstikov

  target 'm.tolstikovTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'm.tolstikovUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
