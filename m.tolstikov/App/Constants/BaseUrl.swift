/* Структура хранения константы базового url */

import Foundation

struct BaseUrl {
    
    let baseUrl = URL(
        // swiftlint:disable:next force_unwrapping
        string: "http://localhost:8080/")!

}
